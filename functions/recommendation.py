

def find_recommendation(n=5, exploration=1.0):
    """
    This is a function that finds top n recommendation based on the level of exploration specified by users.
    :param
        n (int): number of recommendations
        exploration (float): degree of exploration (0.0 = being adventurous, 1.0 = balanced, 2.0 = most similar)
    :return:
        top_n (list): a list of recommendation, first being the most optimal
    """
    top_n = []

    top_n = [1, 2, 3, 4, 5]

    return top_n
